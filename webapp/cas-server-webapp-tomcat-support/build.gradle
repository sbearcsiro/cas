apply plugin: "java"
apply from: project.file("../gradle/webapp-dependencies.gradle")

description = "Apereo CAS Web Application support for Apache Tomcat"

dependencies {
    implementation libraries.springboottomcat
}
